name: generators
description: "A new Flutter project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.5.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  build: ^2.4.1
  source_gen: ^1.5.0
  analyzer: ^6.7.0
  fast_bloc_annotations: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.4.13

  flutter_lints: ^2.0.0